dist/
*.zip
*.log
node_modules/
.wrangler/
__pycache__/
# generated Facebook result cards (fb_cards.py)
media/cards/
# match-analysis briefs are per-run scratch (match_brief.py)
data/briefs/
# reels are generated video (fb_reel.py) - rendered on the runner, never stored
media/reels/
# The Oracle side copy's exports (oracle-yalla/predict.cmd) - still written on
# the laptop for the user's APEX work, but no longer part of the site since
# 2026-09-24 (one model, python; results_archive.py took over the frozen
# results). Ignored so the laptop can never push them back into the build.
data/oracle_predictions.json
data/oracle_results.json
# The site's WORKING DATA lives in the data store (Workers KV), not in git
# (step 3, 2026-09-24 - data_store.py). `python data_store.py pull` fills these
# in; every workflow that reads them pulls first. Keep this list equal to
# data_store.STORE_FILES (tests/test_data_store.py checks it).
data/matches.json
data/matches_archive.json
data/fixtures.json
data/standings.json
data/scorers.json
data/assists.json
data/goal_events.json
data/match_details.json
data/headlines.json
data/fetch_debug.json
data/results_archive.json
data/reels_auto.json
data/.store_state.json
